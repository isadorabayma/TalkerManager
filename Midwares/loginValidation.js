const emailVal = (email) => {
    if (!email) {
        console.log('n tem email');
    }
};

const passwordVal = (password) => {
    if (!password) {
        console.log('n tem senha');
    }
};

module.exports = {
    emailVal,
    passwordVal,
};
